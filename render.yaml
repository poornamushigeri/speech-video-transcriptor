services:
  - type: web
    name: speech-video-transcripter-summariser
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: streamlit run main_app.py --server.port 10000 --server.address 0.0.0.0
